person_elements:
  !include person_template.yaml

title_button_card:
  !include title_template.yaml

sensibo_card:
  !include sensibo_template.yaml

harmony_elements:
  !include harmony_elements_template.yaml